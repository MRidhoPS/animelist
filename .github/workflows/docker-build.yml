name: Build Docker Image on Code Change

on:
  push:
    branches:
      - main  # Jalankan workflow saat push ke branch 'main'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 🛒 Checkout code
        uses: actions/checkout@v3

      - name: 🐳 Set up Docker
        uses: docker/setup-buildx-action@v3

      - name: 🧱 Build Docker image
        run: docker build -t animelist .

      # Optional - Push to DockerHub
      # - name: 🔐 Login to DockerHub
      #   run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin

      # - name: 📤 Push Docker image
      #   run: docker tag my-nextjs-app ${{ secrets.DOCKER_USERNAME }}/my-nextjs-app:latest && docker push ${{ secrets.DOCKER_USERNAME }}/my-nextjs-app:latest
